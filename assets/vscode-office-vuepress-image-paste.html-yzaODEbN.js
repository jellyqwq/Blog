import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as s,c as a,e as n}from"./app-yROkIB6x.js";const i={},d=n(`<h1 id="vs-code-粘贴图片到-vuepress-public-目录" tabindex="-1"><a class="header-anchor" href="#vs-code-粘贴图片到-vuepress-public-目录" aria-hidden="true">#</a> VS Code 粘贴图片到 VuePress public 目录</h1><p>这次想解决的问题很简单：写 Markdown 的时候直接粘贴图片，让图片文件跟着博客仓库一起提交到 GitHub，不再额外准备图床。</p><p>我的博客是 VuePress，静态资源放在：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>所以理想状态是，图片保存到：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public/images/文章目录/文章名/图片名.png
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Markdown 里插入的路径则保持为：</p><div class="language-markdown line-numbers-mode" data-ext="md"><pre class="language-markdown"><code><span class="token url"><span class="token operator">!</span>[<span class="token content">图片</span>](<span class="token url">/images/文章目录/文章名/图片名.png</span>)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这样本地写作、GitHub 提交、VuePress 构建后的访问路径都能统一起来。</p><h2 id="误判" tabindex="-1"><a class="header-anchor" href="#误判" aria-hidden="true">#</a> 误判</h2><p>一开始以为是 Markdown All in One 在处理粘贴图片，因为这个插件名字实在太像会管 Markdown 图片。</p><p>后来卸载 Markdown All in One 以后，所见即所得编辑器还能继续用，粘贴图片也还能写入目录，才确认它不是关键角色。</p><p>真正接管 Markdown 所见即所得编辑和粘贴图片的是 Office Viewer，也就是 <code>vscode-office</code>。它内部使用了 Vditor 作为编辑器，所以看到 Vditor 仓库时也很容易以为问题在 Vditor。</p><p>实际链路是：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Office Viewer / vscode-office -&gt; 内嵌 Vditor -&gt; 处理粘贴图片 -&gt; 写入本地文件
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Vditor 负责编辑器和上传入口，但把图片写入哪个本地路径，是外层 VS Code 扩展 <code>vscode-office</code> 做的。</p><h2 id="只改配置的边界" tabindex="-1"><a class="header-anchor" href="#只改配置的边界" aria-hidden="true">#</a> 只改配置的边界</h2><p><code>vscode-office</code> 提供了这两个配置：</p><div class="language-json line-numbers-mode" data-ext="json"><pre class="language-json"><code><span class="token punctuation">{</span>
  <span class="token property">&quot;vscode-office.pasterImgPath&quot;</span><span class="token operator">:</span> <span class="token string">&quot;\${workspaceDir}/src/.vuepress/public/images/\${relativeDir}/\${fileName}/\${now}.png&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;vscode-office.workspacePathAsImageBasePath&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>其中 <code>workspaceDir</code>、<code>fileName</code>、<code>now</code> 是插件原本支持的变量。</p><p>但是我想要的路径里还有一段“文章相对 <code>src</code> 的目录”，例如：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/life/journal/2026-05-07.md
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>对应图片目录应该是：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public/images/life/journal/2026-05-07/
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>也就是需要一个类似 <code>\${relativeDir}</code> 的变量。</p><p>问题就在这里：<code>vscode-office</code> 原生不支持这个变量。只改 <code>.vscode/settings.json</code>，没有办法动态拿到文章所在目录。</p><h2 id="本地补丁" tabindex="-1"><a class="header-anchor" href="#本地补丁" aria-hidden="true">#</a> 本地补丁</h2><p>最后是在本机安装的扩展文件里补了逻辑：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>~/.vscode/extensions/cweijan.vscode-office-3.5.4/out/extension.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>补丁做了三件事：</p><ol><li>根据当前 Markdown 文件计算相对 <code>src</code> 的目录。</li><li>支持在 <code>vscode-office.pasterImgPath</code> 里使用 <code>\${relativeDir}</code>。</li><li>插入 Markdown 时，把真实文件路径转换成 VuePress 能识别的 <code>/images/...</code> 路径。</li></ol><p>最终写入效果类似：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public/images/life/journal/2026-05-07/1778721352216.png
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Markdown 中保存为：</p><div class="language-markdown line-numbers-mode" data-ext="md"><pre class="language-markdown"><code><span class="token url"><span class="token operator">!</span>[<span class="token content">1778721352216</span>](<span class="token url">/images/life/journal/2026-05-07/1778721352216.png</span>)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这样提交到 GitHub 后，图片仍然在仓库里，VuePress 构建时也能正常访问。</p><h2 id="本地预览问题" tabindex="-1"><a class="header-anchor" href="#本地预览问题" aria-hidden="true">#</a> 本地预览问题</h2><p>图片路径保存成 <code>/images/...</code> 后，VuePress 网站可以识别，但 Office Viewer 的本地 WebView 不知道 <code>/images</code> 应该对应：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public/images
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>于是出现了一个新问题：图片文件已经存对了，Markdown 路径也对了，但本地所见即所得编辑器里看不到图片。</p><p>这个问题不能通过改 Markdown 路径解决。因为如果把 Markdown 改成本机绝对路径，网站构建就不干净了。</p><p>最后的做法是继续补 Office Viewer 的 WebView 显示逻辑：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>~/.vscode/extensions/cweijan.vscode-office-3.5.4/resource/vditor/index.js
~/.vscode/extensions/cweijan.vscode-office-3.5.4/resource/vditor/util.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>让它只在本地预览时把：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>/images/...
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>临时映射到：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>src/.vuepress/public/images/...
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Markdown 文件里的内容不变，仍然保持网站友好的 <code>/images/...</code>。</p><h2 id="最终配置" tabindex="-1"><a class="header-anchor" href="#最终配置" aria-hidden="true">#</a> 最终配置</h2><p>项目里的 <code>.vscode/settings.json</code> 保留这些配置：</p><div class="language-json line-numbers-mode" data-ext="json"><pre class="language-json"><code><span class="token punctuation">{</span>
  <span class="token property">&quot;editor.pasteAs.enabled&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
  <span class="token property">&quot;markdown.editor.filePaste.enabled&quot;</span><span class="token operator">:</span> <span class="token string">&quot;always&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;markdown.editor.filePaste.copyIntoWorkspace&quot;</span><span class="token operator">:</span> <span class="token string">&quot;mediaFiles&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;markdown.copyFiles.overwriteBehavior&quot;</span><span class="token operator">:</span> <span class="token string">&quot;nameIncrementally&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;markdown.updateLinksOnFileMove.enabled&quot;</span><span class="token operator">:</span> <span class="token string">&quot;prompt&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;vscode-office.pasterImgPath&quot;</span><span class="token operator">:</span> <span class="token string">&quot;\${workspaceDir}/src/.vuepress/public/images/\${relativeDir}/\${fileName}/\${now}.png&quot;</span><span class="token punctuation">,</span>
  <span class="token property">&quot;vscode-office.workspacePathAsImageBasePath&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>真正对 Office Viewer 生效的是后两项。</p><p>前几项是 VS Code 原生 Markdown 粘贴配置，保留着不冲突，但在 Office Viewer 接管粘贴时不是主流程。</p><h2 id="注意事项" tabindex="-1"><a class="header-anchor" href="#注意事项" aria-hidden="true">#</a> 注意事项</h2><p>这是一个本机扩展补丁，不是 <code>vscode-office</code> 官方能力。</p><p>如果以后出现下面几种情况，可能需要重新补：</p><ol><li>VS Code 扩展更新。</li><li>重新安装 Office Viewer。</li><li>换了一台电脑。</li><li><code>~/.vscode/extensions/cweijan.vscode-office-版本号/</code> 目录变化。</li></ol><p>每次改完扩展文件后，都需要在 VS Code 里执行：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Developer: Reload Window
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>也就是：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Cmd + Shift + P -&gt; Developer: Reload Window
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>否则扩展代码不会立刻重新加载。</p><h2 id="复盘" tabindex="-1"><a class="header-anchor" href="#复盘" aria-hidden="true">#</a> 复盘</h2><p>这次真正有用的判断是：先确认到底是谁在处理粘贴。</p><p>Markdown All in One 只是一个容易被误伤的名字；Vditor 是编辑器内核；真正决定图片保存路径的是 Office Viewer 外层扩展。</p><p>想让图片路径同时满足三件事：</p><ol><li>图片文件进入 Git 仓库。</li><li>VuePress 构建后能访问。</li><li>VS Code 本地所见即所得能预览。</li></ol><p>就不能只盯着 Markdown 链接本身，还要同时处理“保存路径”和“本地预览路径映射”这两层。</p><p>最终比较顺手的状态是：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>文章路径: src/life/journal/2026-05-07.md
图片文件: src/.vuepress/public/images/life/journal/2026-05-07/xxx.png
文章引用: /images/life/journal/2026-05-07/xxx.png
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这算是折腾了一圈之后比较干净的平衡点。</p>`,71),o=[d];function r(t,c){return s(),a("div",null,o)}const u=e(i,[["render",r],["__file","vscode-office-vuepress-image-paste.html.vue"]]);export{u as default};
